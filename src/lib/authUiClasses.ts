/** Tombol aksi pada kartu auth/onboarding — selaras DocumentKnowledgeManagement (View/Delete Document). */
export const authCardButtonClass =
  'h-10 w-full justify-center gap-2 rounded-xl text-sm whitespace-normal leading-snug px-3'

/** Varian setengah lebar (Back + Continue berdampingan). */
export const authCardButtonInlineClass =
  'h-10 flex-1 justify-center gap-2 rounded-xl text-sm whitespace-normal leading-snug px-3'

/** Input pada kartu auth/onboarding — tinggi selaras tombol (h-10). */
export const authCardInputClass = 'h-10 rounded-xl px-3 text-sm'
